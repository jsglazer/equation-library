{
	"name": "equation-library",
	"version": "1.0.0",
	"description": "Pop-up library of custom math formulas with a live LaTeX generator, editor autocomplete and a JSON catalog.",
	"main": "main.js",
	"scripts": {
		"dev": "node scripts/build-styles.mjs && node esbuild.config.mjs",
		"build": "node scripts/build-styles.mjs && tsc --noEmit -p tsconfig.json && node esbuild.config.mjs production",
		"styles": "node scripts/build-styles.mjs",
		"test": "vitest run",
		"test:watch": "vitest"
	},
	"keywords": ["obsidian", "plugin", "math", "latex", "mathlive"],
	"author": "Josh Glazer",
	"license": "MIT",
	"dependencies": {
		"mathlive": "^0.110.0"
	},
	"devDependencies": {
		"@types/node": "^25.9.3",
		"esbuild": "0.28.1",
		"obsidian": "latest",
		"tslib": "2.8.1",
		"typescript": "^6.0.3",
		"vitest": "^4.1.8"
	}
}
